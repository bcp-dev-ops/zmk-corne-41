/*

 ┌─────────────────────────────────────────── NUMBER ────────────────────────────────────────┐
 │                                                                                           │
 │   ╭────────────── LEFT HAND ───────────────╮    ╭────────────── RIGHT HAND ───────────────╮
 │                                                                                           │
 │   Top:     1     2     3     4     5             6     7     8     9     0                │
 │   Home:    1     2     3     4     5             6     7     8     9     0                │
 │           │C│   │A│   │G│   │S│                       │S│   │G│   │A│   │C│               │
 │   Bottom:  1     2     3     4     5             6     7     8     9     0                │
 │                                                                                           │
 │   Thumbs: [REPEAT] [DELETE] [ESC]                [ENTER] [SPACE] [REPEAT]                 │
 │              ^left cluster^                           ^right cluster^                     │
 │                                                                                           │
 │  ──────────────────────────────────────────────────────────────────────────────────────── │
 │  Legend:                                                                                  │
 │   &kp <KEY>     → normal keypress                                                         │
 │   &hm MOD <KEY> → tap = key, hold = modifier                                              │
 │   &lt LAYER <KEY> → tap = key, hold = activate layer                                      │
 │   &magic_key     → custom macro / layer trigger                                           │
 │   [REPEAT]       → repeats last key or macro                                              │
 │                                                                                           │
 └───────────────────────────────────────────────────────────────────────────────────────────┘
*/

 / {
    numbers: numbers {
        bindings = <

            /* Top Row */
            /* ------------------------LEFT HAND --------------------------------------------*/   /* ------------------------RIGHT HAND --------------------------------------------*/
            &kp N1             &kp N2           &kp N3          &kp N4            &kp N5             &kp N6       &kp N7            &kp N8          &kp N9          &kp N0


            /* Home Row */
            /* ------------------------LEFT HAND --------------------------------------------*/   /* ------------------------RIGHT HAND --------------------------------------------*/
            &hm LCTRL N1       &hm LALT N2      &hm LGUI N3     &hm LSHIFT N4     &kp N5             &kp N6       &hm RSHIFT N7     &hm RGUI N8     &hm RALT N9     &hm RCTRL N0


            /* Bottom Row */
            /* ------------------------LEFT HAND --------------------------------------------*/   /* ------------------------RIGHT HAND --------------------------------------------*/
            &kp N1             &kp N2           &kp N3          &kp N4            &kp N5             &kp N6       &kp N7            &kp N8          &kp N9          &kp N0


            /* Thumbs */
            /* ------------------------LEFT HAND --------------------------------------------*/   /* ------------------------RIGHT HAND --------------------------------------------*/

                                                     &kp RPT     &lt NAV BSPC    &lt SYM ESC        &lt FN ENTER    &lt MAGIC SPACE   &magic_key
        >;
    };
};

